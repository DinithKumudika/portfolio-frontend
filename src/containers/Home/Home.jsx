import React from 'react';
import './Home.scss';

const Home = ({title, id}) => {
     return (
          <div id={id} className='flex w-full h-screen bg-white'>Home this is the home page</div>
     );
}

export default Home;