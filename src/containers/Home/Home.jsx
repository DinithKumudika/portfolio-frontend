import React from 'react';
import './Home.scss';

const Home = ({title, id}) => {
     return (
          <div id={id} className='text-white'>Home this is the home page</div>
     );
}

export default Home;